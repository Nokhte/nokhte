import 'package:leopard_flutter/leopard_transcript.dart';

class Leopard {
  static List<LeopardWord> get response => [
        LeopardWord('resulting', 0.0, 0.1, 1.00),
        LeopardWord('words', 0.2, 0.3, 1.00),
      ];
}
