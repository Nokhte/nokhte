import 'package:flutter/material.dart';

class WifiDisconnectOverlay extends StatelessWidget {
  const WifiDisconnectOverlay({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
