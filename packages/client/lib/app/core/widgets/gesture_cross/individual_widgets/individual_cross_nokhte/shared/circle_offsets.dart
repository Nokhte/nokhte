import 'package:flutter/material.dart';

class CircleOffsets {
  static Offset get center => const Offset(.5, -4);

  static Offset get left => const Offset(-26, -4);
  static Offset get right => const Offset(26, -4);

  static Offset get top => const Offset(-.5, 28);
  static Offset get bottom => const Offset(.5, 21);
}
