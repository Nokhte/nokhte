import 'package:flutter/material.dart';

class CircleOffsets {
  static Offset get bottom => const Offset(0, 12);
  static Offset get center => const Offset(0, 35);
  static Offset get left => const Offset(23, 35);
  static Offset get right => const Offset(-23, 35);
  static Offset get top => const Offset(0, 57);
}
