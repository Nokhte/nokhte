import 'package:flutter/material.dart';

class TimeAlignmentModelWidget extends StatelessWidget {
  const TimeAlignmentModelWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
