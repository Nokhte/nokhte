import 'package:flutter/material.dart';

mixin LetEmCookConstants {
  static List<Color> greenGrad(double opacity) => [
        const Color(0xFF75FF6F).withOpacity(opacity),
        const Color(0xFF5BFF84).withOpacity(opacity),
      ];
}
