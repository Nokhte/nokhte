import 'package:flutter/widgets.dart';

class CollaboratorPoolScreen extends StatelessWidget {
  const CollaboratorPoolScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
