import 'package:flutter/material.dart';

class IrlNokhteSessionNotesPlaceHolderScreen extends StatelessWidget {
  const IrlNokhteSessionNotesPlaceHolderScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Notes"),
      ),
    );
  }
}
