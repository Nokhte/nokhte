name: primala_backend
description: A sample command-line application.
version: 1.0.0
publish_to: none
# repository: https://github.com/my_org/my_repo
environment:
  # sdk: '>=2.19.6 <3.0.0'
  sdk: ">=3.0.0 <4.0.0"

# Add regular dependencies here.
dependencies:
  dotenv: ^4.1.0
  equatable: ^2.0.5
  flutter:
    sdk: flutter
  supabase_flutter: ^1.10.9
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^2.0.0
  mockito: ^5.4.0
