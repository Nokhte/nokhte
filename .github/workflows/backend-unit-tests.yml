name: backend-unit-tests
run-name: Backend Unit Tests
on:
  push:
    branches:
      - development
    paths:
      - "packages/backend/**"
  pull_request:
    branches:
      - development
    paths:
      - "packages/backend/**"

      
jobs:
  run-tests:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: packages/backend
    steps:
      - uses: actions/checkout@v4
      - uses: supabase/setup-cli@v1
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.13.9"
          channel: "stable"
      - run: echo "export GOOGLE_CLIENT_ID='${{secrets.GOOGLE_CLIENT_ID}}'" >> .env
      - run: echo "export GOOGLE_CLIENT_SECRET='${{secrets.GOOGLE_CLIENT_SECRET}}'" >> .env
      - run: supabase start
      - run: supabase start
      - run: supabase functions serve
      - run: cd dart && flutter test
